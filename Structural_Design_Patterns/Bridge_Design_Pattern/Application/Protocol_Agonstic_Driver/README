This example shows how the Bridge pattern enables protocol agonostic driver for implementation of different protocols having similar implementations. 
UVM example that demonstrates how the Bridge pattern enables a single uvm_driver to support multiple protocol variants (e.g., Ethernet 10G vs. 25G) 
using configuration-based injection—without factory overrides—so the same sequence and test work across all variants.